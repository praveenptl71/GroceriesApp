export interface IProduct{
    title: string,
    description?: string,
    price: string,
    quantity: string,
    image: any
}

export interface ICategory{
    title: string,
    image: any,
    bgColor: string
}